{
  "name": "openember-plugins",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenClaw plugins for multi-user memory and user profile management",
  "workspaces": [
    "openember-memory",
    "openember-user-profiles"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
