{
  "name": "openember-plugins",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenClaw plugins for OpenEmber",
  "workspaces": [
    "openember-memory"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
